[project]
name = "flask-tic-tac-toe"
version = "0.1.2"
description = "Tic-Tac-Toe game using Flask"
authors = [
    {name = "John R"}
]
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
]

[dependency-groups]
dev = [
    "flask (>=3.1.2,<4.0.0)",
    "setuptools (>=80.9.0,<81.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
